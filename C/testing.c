#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50}; // Declare and initialize an array
    int i;

    // Print array elements
    printf("Array elements:\n");
    for (i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
